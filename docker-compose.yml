version: '3'

volumes:
  geop-mongo:
  geop-redis:

services:
  mongo:
    image: mongo:3
    volumes:
      - geop-mongo:/data/db
    ports:
      - 27017:27017

  mongo-express:
    image: mongo-express:latest
    links:
      - mongo:mongo
    ports:
      - 8081:8081

  redis:
    image: redis:3.2
    volumes:
      - geop-redis:/data
    ports:
      - 6379:6379
